# module-login-m payload

This payload contains reusable login module assets:
- backend auth service
- frontend login pages
- docker deployment templates
- shared types
- tests and handover docs

Primary entrypoints:
- Backend app: `backend/login_m_backend/main.py`
- Frontend page: `frontend/index.html`
- Deploy compose: `deploy/docker-compose.test.yml`
