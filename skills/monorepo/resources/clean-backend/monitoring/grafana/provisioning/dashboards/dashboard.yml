# Grafana 仪表板配置（自动加载）
apiVersion: 1

providers:
  - name: 'Tool Server'
    orgId: 1
    folder: 'Tool Server'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

